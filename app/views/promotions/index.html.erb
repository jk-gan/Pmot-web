<h1>Promotions#index</h1>
<p>Find me in app/views/promotions/index.html.erb</p>

<table>
  <% @promotions.each do |p| %>
  <tr>
    <td><%= p.name %></td>
    <td><%= p.description %></td>
    <td><%= p.starts_at.strftime("%d %B %Y") %></td>
    <td><%= p.expires_at.strftime("%d %B %Y") %></td>
  </tr>

  <% end %>
</table>


<%= link_to 'Add new', new_promotion_path %>
<%= link_to 'Home', root_path %>
